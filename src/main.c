#include <stdio.h>
#include "read_&write.h"

int main(){
    char_pointer_array result = read_strings_from_file("test.bin");
    /*for(size_t i = 0; i < 1; i++){
        printf(result.text[i]);
    }*/
}